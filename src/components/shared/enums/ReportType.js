/*export const ReportType = ["INVENTORY", "USERS", "ACTIVITY_LOG", "ORDERS"];*/

export const ReportType = Object.freeze({
    "INVENTORY":INVENTORY,
    "USERS":USERS,
    "ACTIVITY_LOG":ACTIVITY_LOG,
    "ORDERS":ORDERS
});